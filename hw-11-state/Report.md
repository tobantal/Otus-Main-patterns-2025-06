# 📊 Итоговый отчет по ДЗ-11: Смена режимов обработки команд

## 🎯 Цель работы

Применить паттерн **Состояние** для изменения поведения обработчиков команд в многопоточной системе, с корректной реализацией потокового исполнения и управления состояниями через публичные методы `CommandExecutor`.

---

## ✅ Выполненные требования

### Основные требования

* ✅ **Паттерн Состояние** реализован через интерфейс `IExecutorState`
* ✅ **Метод handle** реализован без возврата следующего состояния; команды сами управляют переходами
* ✅ **Состояние "Обычное"** (`NormalState`) — команды выполняются напрямую
* ✅ **Состояние "MoveTo"** (`MoveToState`) — команды перенаправляются в другую очередь
* ✅ **HardStopCommand** корректно завершает поток через установку состояния в `nullptr`
* ✅ **Начальное состояние** задаётся через конструктор `CommandExecutor`
* ✅ **CommandExecutor** управляет жизненным циклом потока через публичные методы `start()`, `join()`

### Реализованные команды

* ✅ **StateChangeCommand** — базовый класс для всех команд смены состояния
* ✅ **HardStopCommand** — остановка исполнения (setState(nullptr))
* ✅ **MoveToCommand** — перевод исполнителя в `MoveToState` с указанием целевой очереди
* ✅ **RunCommand** — возврат в `NormalState`

### Тесты согласно критериям

* ✅ После `HardStopCommand` поток корректно завершается
* ✅ После `MoveToCommand` поток переходит в `MoveToState`
* ✅ После `RunCommand` поток возвращается в `NormalState`
* ✅ Исполнение команд и смена состояния происходят через публичные методы `CommandExecutor`
* ✅ Код не зависит от конкретных реализаций очередей и команд (работа через интерфейсы)

---

## 🏗️ Архитектура решения

### Ключевые особенности

1. **CommandExecutor**

   * Упрощён: удалён флаг `running_`; остановка определяется `nullptr` состояния
   * Цикл `executionLoop()` теперь внутренний и запускается через публичный метод `start()`
   * Публичные методы для управления потоком: `start()`, `join()`

2. **Чистые состояния**

   * `NormalState::handle()` выполняет команду через `command->execute()`
   * `MoveToState::handle()` перекладывает команды в другую очередь

3. **Команды управления**

   * Все наследуются от `StateChangeCommand`
   * В `execute()` вызывают `executor->setState(targetState_)`
   * Различаются только целевым состоянием

### Диаграмма классов

```
ICommand <|-- StateChangeCommand
StateChangeCommand <|-- HardStopCommand
StateChangeCommand <|-- MoveToCommand  
StateChangeCommand <|-- RunCommand

IExecutorState <|-- NormalState
IExecutorState <|-- MoveToState

CommandExecutor --> IExecutorState
StateChangeCommand --> CommandExecutor
```

---

## 📁 Структура проекта

```
hw-11-state/
├── CMakeLists.txt
├── include/
│   ├── IExecutorState.hpp         # Интерфейс состояния
│   ├── NormalState.hpp            # Обычное состояние
│   ├── MoveToState.hpp            # Состояние перенаправления
│   ├── StateChangeCommand.hpp     # Базовый класс команд
│   ├── MoveToCommand.hpp          # Команда перехода в MoveTo
│   ├── RunCommand.hpp             # Команда возврата в Normal
│   └── CommandExecutor.hpp        # Исполнитель с публичными методами start/join/setState
├── src/
│   ├── NormalState.cpp
│   ├── MoveToState.cpp
│   ├── StateChangeCommand.cpp
│   ├── MoveToCommand.cpp
│   ├── RunCommand.cpp
│   └── CommandExecutor.cpp
└── tests/
    └── ExecutorStateTest.cpp      # Все тесты теперь используют TEST_F с ExecutorStateTest
```

---

## 🔄 Примеры использования

### Базовое использование

```cpp
auto mainQueue = std::make_shared<ThreadSafeQueue>();
auto backupQueue = std::make_shared<ThreadSafeQueue>();

auto executor = std::make_shared<CommandExecutor>(mainQueue);
executor->start();

// Обычная работа
mainQueue->push(someCommand);

// Переход в MoveTo
mainQueue->push(std::make_shared<MoveToCommand>(executor, backupQueue));

// Возврат в Normal
mainQueue->push(std::make_shared<RunCommand>(executor));

// Остановка
mainQueue->push(std::make_shared<HardStopCommand>(executor));

executor->join();
```

### Кастомное начальное состояние

```cpp
auto initialState = std::make_shared<MoveToState>(archiveQueue);
auto executor = std::make_shared<CommandExecutor>(mainQueue, initialState);
executor->start();
executor->join();
```

---

## 🧪 Результаты тестирования

Все тесты теперь используют `TEST_F(ExecutorStateTest, ...)` и проходят:

1. **InitialStateIsNormal** ✅ — проверка начального состояния `NormalState`
2. **CustomInitialState** ✅ — кастомное начальное состояние
3. **NormalStateExecutesCommands** ✅ — команды выполняются в NormalState
4. **MoveToStateRedirectsCommands** ✅ — перенаправление команд
5. **HardStopTerminatesThread** ✅ — поток корректно завершается
6. **MoveToCommandChangesStateToMoveTo** ✅ — переход в MoveToState
7. **RunCommandChangesStateToNormal** ✅ — возврат в NormalState
8. **ComplexStateTransitionScenario** ✅ — сложные сценарии переходов
9. **InterfaceIndependence** ✅ — независимость от конкретных реализаций очередей и команд

---

## 💡 Преимущества решения

1. **Простота и ясность**

   * Потоковая логика скрыта внутри `CommandExecutor`
   * Команды делают только одно действие
2. **Соответствие SOLID**

   * **S** — один класс = одна ответственность
   * **O** — добавление состояний без изменения существующих
   * **L** — наследники заменяют базовый класс
   * **I** — минимальные интерфейсы (`IExecutorState`, `ICommand`)
   * **D** — зависимости через абстракции
3. **Расширяемость**

   * Легко добавить новые состояния и команды
   * Можно начинать исполнение с любого состояния

---

## 🎓 Компетенции

* ✅ Паттерн **Состояние** (State)
* ✅ Паттерн **Команда** (Command)
* ✅ Принципы **SOLID**
* ✅ Многопоточное программирование
* ✅ Unit-тестирование с Google Test

---

## 📊 Оценка

| Критерий                                | Баллы  | Выполнено |
| --------------------------------------- | ------ | --------- |
| ДЗ сдано на проверку                    | 2      | ✅         |
| Код опубликован на GitHub/GitLab        | 1      | ✅         |
| Настроен CI                             | 2      | ✅         |
| Код компилируется без ошибок            | 1      | ✅         |
| Тест: после HardStop поток завершается  | 1      | ✅         |
| Тест: после MoveToCommand → MoveTo      | 1      | ✅         |
| Тест: после RunCommand → Normal         | 1      | ✅         |
| Код не зависит от конкретных реализаций | 1      | ✅         |
| **Итого:**                              | **10** | **✅**     |

---

## 🚀 Сборка и запуск

```bash
mkdir build
cd build
cmake ..
make -j4

# Запуск тестов
./state_tests

# Через ctest
ctest --output-on-failure
```

---

## 📝 Выводы

* Потоковая обработка команд теперь корректно управляется через публичные методы `CommandExecutor`
* Все состояния реализованы чисто, с минимальной логикой
* Тесты покрывают все критические сценарии
* Решение соответствует принципам **SOLID** и паттерну **State**
* Задание выполнено полностью, все критерии соблюдены
